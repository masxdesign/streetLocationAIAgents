# Cluster Pillar Post Workflow

This directory contains the system and user prompts to power a 4-agent n8n workflow designed to generate comprehensive, SEO-optimized "Cluster Pillar Posts". 

## Workflow Overview

When building out a topic cluster strategy, the **Pillar Post** acts as the central hub that links out to deeper, more specific **Supporting Posts**. This workflow takes your existing supporting content and generates a cohesive, high-quality pillar post formatted in pure Markdown.

The workflow uses four sequential AI agents:
1. **The Content Summarizer:** Distills a full-length supporting post into a 2-3 sentence summary.
2. **The Information Architect (Outline Planner):** Generates a logical Markdown outline grouping the supporting post concepts.
3. **The Content Synthesizer (Draft Writer):** Fills in the outline, writing short contextual summaries and injecting precise placeholder URLs for internal link building.
4. **The SEO Optimizer & Editor:** Polishes the final draft, enforcing strict markdown output and validating that no link placeholders were lost.

---

## Dynamic Placeholders Schema

Inside the `user.md` files for these agents, you will find dynamic placeholders formatted as `[INSERT_...]`. 
When setting up this workflow in automation tools like n8n, you must map your database fields or previous node outputs to these exact spots.

Here are the data schemas and examples for every placeholder:

### 1. `[INSERT_SUPPORTING_POST_TITLE]`
* **Used in:** `1. Content Summarizer`
* **Schema (Data Type):** `String`
* **Description:** The title of a single supporting post being processed.
* **Example:**
  ```json
  "On-Page SEO Basics for 2024"
  ```

### 2. `[INSERT_SUPPORTING_POST_FULL_TEXT]`
* **Used in:** `1. Content Summarizer`
* **Schema (Data Type):** `String`
* **Description:** The raw body content of a single supporting post.
* **Example:**
  ```text
  "Title tags and meta descriptions remain the foundation of on-page SEO. While Google sometimes rewrites them, a well-crafted title tag is your first impression in the SERPs..."
  ```

### 3. `[INSERT_BROAD_CORE_TOPIC]`
* **Used in:** `2. Information Architect`, `3. Content Synthesizer`, `4. SEO Optimizer`
* **Schema (Data Type):** `String`
* **Description:** The overarching focus keyword or title of the pillar post you are generating.
* **Example:**
  ```json
  "Content Marketing Strategy for Startups"
  ```

### 4. `[INSERT_SUPPORTING_POSTS_DATA]`
* **Used in:** `2. Information Architect`, `3. Content Synthesizer`
* **Schema (Data Type):** `JSON Array of Objects` (or formatted String representing this array)
* **Description:** The aggregated output from Agent 1. It must include the specific post `id` so the Synthesizer knows what to place in the `(/placeholder/id/...)` links.
* **Example:**
  ```json
  [
    {
      "id": 101,
      "title": "Long-tail keyword research",
      "summary": "Focuses on finding low-competition keywords using free SEO tools."
    },
    {
      "id": 102,
      "title": "On-Page SEO Basics",
      "summary": "Best practices for optimizing title tags and meta descriptions."
    }
  ]
  ```

### 5. `[INSERT_OUTLINE_FROM_AGENT_1]`
* **Used in:** `3. Content Synthesizer`
* **Schema (Data Type):** `String` (Markdown format)
* **Description:** The raw output generated by Agent 2 (The Information Architect).
* **Example:**
  ```markdown
  # The Ultimate Guide to SEO
  ## 1. Finding Keywords
  ### Long-tail keyword research
  - Understanding intent
  - Free discovery tools
  ```

### 6. `[INSERT_LIST_OF_IDS]`
* **Used in:** `4. SEO Optimizer`
* **Schema (Data Type):** `JSON Array` or Comma-separated `String`
* **Description:** A simple list of the supporting post IDs. The SEO Optimizer uses this to verify that the draft did not drop any of the internal links.
* **Example:**
  ```json
  [101, 102]
  ```

### 7. `[INSERT_DRAFT_FROM_AGENT_2]`
* **Used in:** `4. SEO Optimizer`
* **Schema (Data Type):** `String` (Markdown format)
* **Description:** The raw, unedited text generated by Agent 3 (The Content Synthesizer) that needs to be polished.
* **Example:**
  ```markdown
  # The Ultimate Guide to SEO
  Content marketing is the lifeblood of early growth...
  
  ## 1. Finding Keywords
  Keyword research is how you figure out exactly what your audience wants. 
  Read our full guide on [how to perform long-tail keyword research](/placeholder/id/101).
  ```
